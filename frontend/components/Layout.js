import React from 'react';

const Layout = ({ children }) => {
  return (
    <div>
      {/* Common layout elements like header, footer, navigation */}
      {children}
    </div>
  );
};

export default Layout;
